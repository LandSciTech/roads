#' @include AAAClassDefinitions.R
NULL

#' A plot method for the results of projecting roads


# setMethod("plot", "SimList", 
#           function(x) {
#             plot(x@costSurface)
#             plot(sf::st_geometry(x@roads), add = TRUE)
#             plot(sf::st_geometry(x@landings), add = TRUE)
#           })

